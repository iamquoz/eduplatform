Flop() string
тестовый метод

StLogout()
деаутентифицирует пользователя

StAvailTests() []TestID
сервер сам ведет учёт выданных тестов
при попытке получить невыданный тест, возвращается 403

StGetTest(id TestID) []TaskID
Точно так же пользователю будут доступны только taskid выданных
тестов
но это потом, а в прототипе похуй
бо всё равно ответы не отправляются

StGetTask(id TaskID) Task
получить задание по идентификатору

StAnswerTask(id TaskID, task Task) bool
отправить ответ на задание

StRegister(string)
меняет пароль, токен не изменяется

NewStudent(Name string) (UserID)
создаёт нового студента

RenewStudent(UserID)
сбрасывает пароль студента

AssignTest(UserID, TestID)
добавляет тест студенту

TestPool() map[TestID][]TaskID
возвращает список доступных тестов

UploadTask(TaskID, Task)
загружает на сервер задание

NewTest(Tasks []TaskID) TestID
создаёт новый тест из заданий типа

RenewTest(TestID, Tasks []TaskID) (old []TaskID)
обновляет тест новыми заданиями, старые возвращаются

GetStudents() map[UserID]string
возвращает список всех студентов




